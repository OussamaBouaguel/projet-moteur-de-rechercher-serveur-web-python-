Instruction pour lancer le serveur :

- ouvrir un terminal
- se placer dans le repertoire contenant le fichier web-server.py
- executer la commande py web-server.py
- ouvrir un navigateur et entrez l'url http://localhost/pageA.py

le fichier index_inverse_temp est la pour remplacer le fichier qui doit contenir
les fonctions pour executer la recherche de l'index inversé.
